
export interface boletTipo {
    createdAt?: Date,
    name?: string,
    imageUrl?: string,
    description?: string,
    comestible?: boolean,
    recomendaciones?: string,
    calidad?: string,
    temporada?: string,
    etiquetas?:string[],
    altImagenes?:string[], 
    himeni?: string,
    anillo?: boolean,
    pie?: string,
    vulva?: boolean,
    id?: number,
}